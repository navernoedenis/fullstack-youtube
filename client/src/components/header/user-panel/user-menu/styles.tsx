import styled from "@emotion/styled";
import MuiCard from "@mui/material/Card";

export const UserMenuContainer = styled(MuiCard)`
  position: absolute;
  top: 114%;
  right: 2%;
  width: 300px;
  box-shadow: var(--global-shadow);
  background-color: var(--user-menu-bg);
`;

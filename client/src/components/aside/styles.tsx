import styled from "@emotion/styled";
import MuiBox from "@mui/material/Box";

export const AsideContainer = styled(MuiBox)`
  padding: 0 12px;
  height: calc(100vh - 56px);
  width: var(--aside-width);
`;

export const SignInButtonContainer = styled(MuiBox)`
  padding: 12px;
  padding-right: 0;
`;
